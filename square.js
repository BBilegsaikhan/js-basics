// "\n" white space
function drawSqr(size) {
  let sqrStr = "";
  for (let j = 0; j < 5; j++) {
    for (let i = 0; i < 5; i++) {
      //sqrStr = sqrStr + "*";
      sqrStr += "*";
    }
    //sqrStr = sqrStr + "\n";
    sqrStr += "\n";
  }
  console.log(sqrStr);
}
drawSqr(5);

// \-n exceleer tab gsn ug.
